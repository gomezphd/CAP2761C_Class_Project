/* General Reset */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

/* Body Styling */
body {
    font-family: 'Roboto', sans-serif;
    line-height: 1.6;
    background-color: #f5f5f5; /* Light Grey Background */
    color: #333333; /* Dark Grey Text */
    display: flex;
    flex-direction: column;
    min-height: 100vh;
}

/* Ensure the container takes up the necessary space */
html, body {
    height: 100%;
}

.container {
    flex: 1;
    max-width: 1200px;
    margin: 20px auto;
    padding: 20px;
    background-color: #ffffff;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    border-radius: 10px;
}

/* Header Styling */
header {
    background-color: #002147; /* Oxford Blue */
    color: #ffffff; /* White */
    padding: 1rem 0; /* Increased padding for better spacing */
    text-align: center;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
}

h1 {
    font-size: 2.5rem; /* Increased font size */
    font-weight: 700;
    margin-bottom: 0.5rem; /* Added margin for spacing */
}

.tagline {
    font-size: 1.2rem; /* Slightly increased font size */
    color: #ffffff; /* White */
    margin: 0.5rem 0; /* Adjusted margin */
}

/* Navigation Styling */
nav {
    background-color: #2F3C52; /* Prussian Blue */
    display: flex;
    justify-content: center;
    padding: 0.5rem 0; /* Adjusted padding for better spacing */
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
}

nav a {
    color: #ffffff; /* White */
    text-decoration: none;
    margin: 0 1rem; /* Increased margin for better spacing */
    font-size: 1.2rem; /* Slightly increased font size */
}

nav a:hover {
    color: #4682B4; /* Steel Blue */
    text-decoration: underline;
}

.separator {
    background-color: #2F3C52; /* Prussian Blue */
    height: 2px;
    margin: 1rem 0; /* Added margin for spacing */
}

/* Section Title Styling */
.section-title {
    background-color: #002147; /* Oxford Blue */
    color: #ffffff; /* White */
    padding: 10px;
    border-radius: 8px;
    text-align: center;
    margin-bottom: 20px; /* Adjusted margin */
    font-size: 1.5rem; /* Adjusted font size */
    font-weight: bold;
}

/* Section Content Styling */
.section-content {
    background-color: #D3D3D3; /* Light Gray */
    padding: 20px;
    border-radius: 8px;
    margin-bottom: 20px; /* Adjusted margin */
    color: #333333; /* Dark Grey Text */
    font-size: 1rem; /* Adjusted font size */
}

.section-content p {
    margin-bottom: 1rem; /* Space after the initial paragraph */
}

/* List Styling */
ul {
    list-style-type: none;
    padding-left: 0;
}

ul li {
    padding: 0.5rem 0;
}

ul li span {
    font-weight: bold;
}

ul li ul {
    padding-left: 1rem;
}

/* Link Styling */
a {
    color: #002147; /* Oxford Blue */
    text-decoration: none;
    transition: color 0.3s ease;
}

a:hover {
    color: #4682B4; /* Steel Blue */
}

/* Background Styling */
.background {
    background: linear-gradient(135deg, #002147, #4682B4); /* Oxford Blue to Steel Blue */
    height: 100%;
    width: 100%;
    position: fixed;
    top: 0;
    left: 0;
    z-index: -1;
}

/* Social Icons Styling */
.social-icons {
    display: flex;
    justify-content: center;
    gap: 1rem; /* Adjusted gap for better spacing */
    margin-top: 1rem; /* Adjusted margin */
}

.social-icons a {
    color: #ffffff; /* White */
    font-size: 1.5rem;
}

.social-icons a:hover {
    color: #4682B4; /* Steel Blue */
}

/* Footer Styling */
footer {
    text-align: center;
    padding: 1rem; /* Adjusted padding */
    background-color: #2F3C52; /* Prussian Blue */
    color: #ffffff; /* White */
    position: relative;
    bottom: 0;
    width: 100%;
}

footer p {
    margin: 0.5rem 0; /* Adjusted margin */
}

footer .expertise {
    font-size: 1.1rem; /* Adjusted font size */
    margin-bottom: 0.5rem; /* Adjusted margin */
}

/* Section Specific Styling */
.section {
    padding-top: 3rem;
    margin-top: -3rem;
}

.note {
    color: #627383; /* Payne's Gray */
    font-weight: bold;
}

.important {
    color: #658196; /* Slate Gray */
    font-weight: bold;
}

pre {
    background-color: #ffffff; /* White */
    padding: 10px;
    border: 1px solid #2F3C52; /* Prussian Blue */
    border-radius: 8px;
    color: #000000; /* Black */
    font-size: 1rem; /* Adjusted font size */
}

/* Contact Form Styling */
form {
    display: flex;
    flex-direction: column;
    gap: 1rem; /* Space between form elements */
}

form label {
    font-weight: bold;
}

form input,
form textarea {
    padding: 0.5rem;
    border: 1px solid #ccc;
    border-radius: 5px;
    font-size: 1rem;
}

form button {
    padding: 0.75rem;
    border: none;
    border-radius: 5px;
    background-color: #002147; /* Oxford Blue */
    color: #ffffff; /* White */
    font-size: 1rem;
    cursor: pointer;
}

form button:hover {
    background-color: #4682B4; /* Steel Blue */
}

/* Additional spacing around the form */
.section-content {
    padding: 20px;
}

/* Responsive Design */
@media (max-width: 768px) {
    header h1 {
        font-size: 2rem;
    }

    .container {
        padding: 1rem;
        margin: 1rem;
    }

    nav a {
        font-size: 1rem;
        margin: 0 0.5rem;
    }

    .section-title {
        font-size: 1.2rem;
        padding: 0.5rem;
    }

    .section-content {
        font-size: 0.9rem;
        padding: 1rem;
    }

    .social-icons a {
        font-size: 1.2rem;
    }
}